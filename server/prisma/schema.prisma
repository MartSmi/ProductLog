datasource db {
  provider = "sqlite"
  url      = "file:./data.db?connection_limit=1"
}

generator client {
  provider = "prisma-client-js"
}

model Item {
  name      String
  EAN       String @id
  artNumber String
  quantity  Int
}